_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Anchored to the repo root so it only ignores the top-level bundler dir
# (bundle install --path vendor), NOT the vendored libraries in assets/vendor/.
/vendor/
.bundle/
node_modules/
Gemfile.lock
.DS_Store
*.swp
*.swo
*~
# Large, regenerable design-preview screenshots
designs/previews/
